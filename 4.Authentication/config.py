from tkinter import *
from PIL import ImageTk
import os
from pathlib import Path

BASE_DIR = Path(__file__).resolve().parent


##! Path to the image
# image_path = os.path.join(BASE_DIR, 'assets', 'img', 'bg.jpg')
image_path = os.path.join(BASE_DIR, 'assets', 'img')